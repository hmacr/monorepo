name: Railway Production Environment

on:
  push:
    branches:
      - main
  pull_request:
    types: [closed]
    branches:
      - main

env:
  RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_API_ACCOUNT_TOKEN }}
  RAILWAY_PROD_ENVIRONMENT: "production"
  RAILWAY_PROJECT_ID: "01790802-1289-4cc6-a8b2-02d4d524fa04"
  WEB_SERVICE_ID: "fc0775bc-9d5d-4818-9d27-88a601617d92"
  DOCS_SERVICE_ID: "c9ebf1eb-520b-4a6e-a338-65018f5e1119"

jobs:
  deploy:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Link to project
        run: railway link --project ${{ env.RAILWAY_PROJECT_ID }} --environment ${{ env.RAILWAY_PROD_ENVIRONMENT }}
      - name: Deploy Web
        run: railway up --service ${{ env.WEB_SERVICE_ID }} --environment ${{ env.RAILWAY_PROD_ENVIRONMENT }}
      - name: Deploy Docs
        run: railway up --service ${{ env.DOCS_SERVICE_ID }} --environment ${{ env.RAILWAY_PROD_ENVIRONMENT }}